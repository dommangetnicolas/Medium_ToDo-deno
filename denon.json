{
  "$schema": "https://deno.land/x/denon/schema.json",

  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-write --allow-read --allow-plugin --unstable src/index.ts",
      "desc": "Run the API"
    },
    "dev": {
      "cmd": "deno run --allow-net --allow-write --allow-read --allow-plugin --unstable src/index.ts",
      "desc": "Run the API in development mode"
    }
  }
}
